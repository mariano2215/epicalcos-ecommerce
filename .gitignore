node_modules
dist
.env
.env.local
.DS_Store
*.log

# Local Netlify folder
.netlify
.mcp.json
# Lo escribe el runtime Deno del CLI al correr `netlify dev`: es el cache del
# bootstrap de edge functions, que este repo no usa (netlify/ solo tiene
# functions). Artefacto local de cada máquina, no entra al repo.
deno.lock

# Caché de webp de scripts/import-catalogo-completo.mjs. Evita volver a bajar
# los originales dataless de iCloud en cada corrida; no es un artefacto del sitio.
.cache
